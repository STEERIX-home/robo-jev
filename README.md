# robojev — a real-time System One for decisions in the physical world

robojev is a research project by Steerix Robotics: a **learned judgment model that decides, many times per second, what a robot should do next** — which action to commit to, whether to stop, whether to grip, which path and speed — from a structured description of the world, without generating text.

It is a *System One* model in the sense that TypeSafe introduced with Jev: the input is a typed state plus a set of questions with candidate answers, and the output is one probability distribution per question, read out in a single forward pass. What is new here is the setting. The state is a physical scene that changes every 100 ms, the questions repeat on every tick, and the answers drive a real controller.

> Status (2026-09-19): the whole pipeline that can be verified without a GPU is implemented, reviewed and tested — contracts, data generators, simulator and controller, streaming harness, scripted expert, rollout labels, serialization, a tiny reference model, and a training loop with exact resume. **The backbone is decided (2026-09-20): Qwen3.5-2B on the real stream path with the `fused` tick (body + 10 decision branches in one forward) meets the 10 Hz gate on the DGX Spark GB10 — p95 62.7 ms on the `upper` profile and 60.2 ms over all 40 batch-0 episodes (1,348 ticks; 2 ticks over 100 ms, both E1 intro ticks), and 52.5 / 52.0 ms with the default `all` serving configuration (0 misses) — while the 4B reaches only the 5 Hz budget.** Readout-only (T0) and short LoRA adaptations and Nimble-style zero-shot scores were measured on the same GPU; no cloud training has run yet. **The D1 pilot (2026-09-21, Task P1) ran seven like-for-like runs on one fixed evaluation set and closed Task 5's GPU acceptance.** Its quality verdict was read on the **old population** — 8 of the `ood_dev` split's 24 episodes, 844 ticks, with the donor rotation that 8-episode list implies — and on the aggregate cell: the 4B T0's raw 0.871 was the highest of the seven runs but its margin over its own state-shuffle control the smallest of the five adapted rows (+0.072 = 0.871 − 0.799, against the 2B LoRA's +0.168 and the 2B T0's +0.165 = 0.454 − 0.289), the 4B T0 still answered 0.799 with the goal and the whole state replaced (rule judge 0.821), and every run was within ±0.014 of its own instruction-shuffle control — so the pilot read "no adapted model demonstrates that it reads the goal at this scale". **Task P3 (2026-09-22) re-measured all seven on every episode of the split — 24 episodes / 2,530 ticks — and that sentence has to be said more precisely now.** The metric is the **525 ticks whose expert label is not the tick's own commitment** (the other 2,005 are "repeat your commitment", where a mechanical policy that reads nothing is correct by construction — 0.875 over the whole cell, above six of the seven runs). There, three rows clear all three robustness checks and they are exactly the three full-backbone T1 runs, the fp32-master one by **+0.080 [+0.022, +0.165]** (episode-balanced +0.100 [+0.074, +0.133], and every one of 24 leave-one-episode-out refits excludes zero) — and **essentially all of that margin is one question family**, the **61 `grasp` ticks** where the goal-reading rule judge scores 1.000 and the mechanical baseline 0.000. So a run *does* demonstrate goal reading, on 61 of 2,530 ticks and against the state-shuffle control only. What the pilot said that still stands: **no run beats its instruction-shuffle control in any stratum of any row, so the goal *text* is still unread**, and no row beats its own control on the 210 observation-gate ticks. Part of the old reading was the instrument rather than the model — the state-shuffle control is donor-dependent and 20.7 % of this population's ticks are shuffled against a frozen final state, which is most of why P2's +0.257 reads +0.080 here. **The backbone does not change** — latency selected it and the 4B still fails 10 Hz under every lever — **and neither measurement gives a quality reason to move to 5 Hz**: the 4B T0's primary-stratum margin still contains zero (+0.021 [−0.032, +0.100]) and the 4B LoRA's is negative. Details in the **Task P3** block of the Model row below. **Task R1 (2026-09-22) acted on that sentence by changing the instrument, not the model.** Three tasks of measurement had ended in one finding — *of 2,530 holdout ticks only 61 require reading the instruction, because the model's own input already contained the resolved goal and the episodes contained almost no events*. So R1 (a) **took the resolved goal out of the model's input**: the `goal` line is now `goal v<n>` plus the periodic instruction sentence, the object intro line lost `attr=`, and the sentence itself now names every forbidden and fragile object — the record still carries the structured goal for the expert, the labels and the rule judge, but the model must ground the text (token serializer `ts0.6`, stream format `v0.4`; a test asserts that none of eleven leak markers appears in either layout, on the D0 fixtures **and on a freshly generated episode**). (b) It **made the episodes event-dense**: instructions change 1–5 times, disturbances land inside the episode and aim at the target, the done-tail is 0.3 s instead of 1 s, and the events that used to be thrown away inside a tick are merged — measured over 400 episodes, ticks carrying a world event went **1.6 % → 15.75 %**, goal changes **0.16 % → 1.33 %**, and the `instruction_changed` lines the model actually sees **7 of 57 → 561 of 561**. **Fix round 1 (2026-09-23) rebuilt the corpus before any of this could be read.** R1's first corpus had 27 episodes that spent all 45 s in a hold/observe **limit cycle**: the stall guard fired at `m_hold` = 15 and the same candidate was adopted again on the next tick, so the longest dead run was always exactly 15 and the stall metric returned 0 by construction — while those 27 episodes carried 23.3 % of the ticks and **60.6 % of the reading ticks**. The harness (`h0.9`) now cools the stalled key out of the candidate set, caps the total, trips on target-geometry age and on an arm that has not moved, and when none of that frees it the episode ends as `stall_exhausted` instead of burning the clock — `max_ms` endings 27 → **0**. On the honest corpus (`r1-robot-v0.2`, 400 episodes / 42,509 ticks) the reading stratum outside the done-tail is **4.11 %** of all ticks and **3.32 %** over the 365 completing episodes (target 8 %), and its weighted loss share **11.7 %** (target 30 %) — both missed by about half, and the first corpus's 7.8 % / 20.1 % were the degenerate episodes talking. The knobs are named in the report; `tick_weights` was left alone because it is a training knob. (c) It **added the metrics that measure events**: reaction delay after a goal change or world event, answer stability between events, `q_stop` delay and false-alarm rate, and the full instruction contrast-pair table — all computed for every column, including the rule judge and the mechanical baseline. Two instrument repairs came with it: the instruction-shuffle control is now the headline "does it read the instruction" column (with the shuffle replacing **all three** places the model sees the sentence), and the state-shuffle control's donor clamp — the thing that turned P2's +0.257 into P3's +0.080 — is gone (donors paired by length, the remainder wrapped, zero frozen ticks). Two pathological D1 episodes that ended at `max_ms` (one livelocked for 157 ticks in the observe gate because the arm parked between the camera and its own target) now end **for a stated reason** within ~80 ticks instead of running the clock out. **No trained model is claimed by R1** — the fp32 T1 on the new instrument was R2's job. **Task R2 (2026-09-23) stopped at the resume gate, diagnosed why, and continued on an amended verdict rule.** docs/06 Task 5 requires the resume gate for the *training scope about to be launched* to pass before a long run starts. R2 first closed P3's carried item — the T1 tolerance had been fixed from a single no-restart pair, and the rule was amended (before measuring) to take the worst of **every** measured pair. The third pair, measured on R2's own path, turned out to be the largest of the three (worst |Δloss| **0.076560** against P2's 0.064136 and P3's 0.023574 — a 3.2× spread), and the tolerance was registered at `loss_abs` **0.2** / `loss_rel` **0.08**. The gate (6 steps = 3 + a real process restart + 3) then returned **fail**: every exact criterion passed (sampler position, drawn units, optimizer step count 6 = 6, no missing tensor) and both parameter criteria passed by 15× (5.959e-4 ≤ 0.01; 3.518e-3 ≤ 0.05), but the loss differed by 0.314955 > 0.2. **No long run was started on that verdict.** A diagnostic followed: three **no-restart** 6-step runs of the same config differ pairwise by **0.0786 / 0.3304 / 0.4090**, so the resumed pair's 0.3150 is *smaller* than the worst pair that never restarted — the resume is not broken, the procedure was. The tolerance had been measured on 5-step pairs and used to judge a 6-step comparison, and `max_steps` sets the learning-rate schedule as well as how long a perturbation has to compound. On that evidence, and with the user's explicit approval, the `t1` verdict was moved onto the criteria that discriminate — the exact ones and the parameter ones — with the loss kept as a reported diagnostic beside its measured spread; **the registered loss numbers were not loosened, they were removed from the verdict**, and the re-judgement used no GPU. Stage B then ran on `--gate t1` → `pass`. The procedure defect that remains open for R3 is the first half: baseline pairs must use the same `max_steps` as the comparison they license. **And Stage B answered the question three tasks of measurement had been sharpening.** The fp32-master T1, trained for **one epoch** (233 steps, 4.3 h, loss 2.672 → 0.200) on the repaired corpus, scores **0.762** on the decision cell's primary stratum — the 235 `ood_dev` ticks across all 26 sealed episodes whose expert label is not that tick's own commitment — against **0.651** for the same model on the same ticks with only the instruction sentence replaced. **The instruction-shuffle margin is +0.111, paired episode-clustered 95 % interval [+0.065, +0.158]: it excludes zero.** All 26 leave-one-episode-out refits exclude zero (worst +0.095 [+0.056, +0.133]), the non-selecting second cell (`dev`, 42 episodes, 276 ticks) reads **+0.101 [+0.059, +0.150]** — a replication on a larger population, **not an independent claim**, because R2's fully-trained tiny scorer marks `robot/dev`'s `q_main` `state_shuffle_irrelevant` (0.7488 against 0.7512 with the whole state replaced) where `robot/ood_dev` carries no mark — and the margin is concentrated where reading is actually tested — the 97 `grasp` ticks, rule judge 0.959 and mechanical baseline 0.000, read **+0.258 [+0.144, +0.373]**. Training is what did it: the same recipe at **40 steps** gives +0.009 [−0.007, +0.027] and the 200-step readout-only T0 gives −0.004 [−0.035, +0.029], all three on one evaluation-set hash. The model also clears both baselines on the whole cell for the first time (0.982 against the mechanical 0.931 and the rule judge 0.812), reacts to a goal change **on the tick it happens** 60 % of the time with zero censoring, and stops flapping — switch rate 0.420 → **0.016**, round trips 477 → **1** — while `q_stop` fires for the first time — but on **1 of its 10 stop onsets**, the other **9 censored (90 %)**; the delay on the one it caught is 2 ticks and false alarms are 0.00 % — and unsafe actions fall 2.25 % → 0.92 % (26 of the 28 remaining unsafe ticks are `stop_ignored`, which is that same censoring seen from the safety side). Four things it does not say: the instruction contrast-pair table still cannot judge anything (`false_change` is null by construction, and the pairs are single records, an input shape the stream-trained checkpoint collapses on — `boolean` 0.272, sensitivity 0.000); `grasp` at 0.629 is a long way under its 0.959 ceiling; `place` (7 ticks) and `push` (5) are too small to read; and it is one seed on one corpus that still misses R1's density target by half. The 4B readout-only **does** clear its own instruction-shuffle control (+0.043 [+0.012, +0.077]) where the 2B readout-only does not, so R1's zero-shot gap was real — but it is 2.6× smaller than one epoch of 2B training, its raw 0.413 is below the rule judge, and it flaps (0.269, 313 round trips). **The backbone decision does not move**, and for the first time the quality evidence points the same way latency already did — with one thing said plainly: **a 4B T1 was never run and cannot be run on this box** (its optimizer-step floor with fp32 masters is 94.0 GiB against the 73 GiB fence, before any activation — Task P2 closed it by arithmetic), so this is a trained-2B against a readout-only-4B comparison, not a like-for-like quality result. The only like-for-like pair, 2B T0 against 4B T0, splits: the 2B is ahead on raw primary accuracy (0.511 vs 0.413), the 4B on the instruction-shuffle margin. **Task R3a (2026-09-24) asked whether that +0.111 survives the seed and whether a second epoch moves `grasp`, changing the seed and the step count and nothing else — same corpus, same harness, same evaluation cells, same two eval-set hashes.** Two more one-epoch runs at **seed 18** and **seed 19** read the primary stratum at **0.889** and **0.804** against their own instruction-shuffled selves, margins **+0.247 [+0.179, +0.308]** and **+0.128 [+0.084, +0.175]** — so with seed 17's +0.111 [+0.065, +0.158] **all three exclude zero, and "this model reads the instruction" is not seed-dependent.** All 234 leave-one-episode-out refits (26 episodes × 3 controls × 3 seeds) exclude zero, and the non-selecting second cell replicates the sign at every seed (+0.101 / +0.261 / +0.156, with the standing discount that its `q_main` is marked `state_shuffle_irrelevant`). **The size does not replicate**: the three margins span **+0.111 to +0.247** — a range wider than the smallest of them — and the three primary accuracies span 0.762 to 0.889. **No interval is computed from the three seeds**; three draws of a training seed are not a sample the recipe's margin can be estimated from, and a three-point interval would be narrower than the honest uncertainty. The sign and the zero-exclusion replicate; the magnitude is known to within about a factor of two. On the second question, a **two-epoch** run at seed 17 (466 steps) lifts `grasp` from **0.629 to 0.845** against the goal-reading rule judge's 0.959 ceiling — closing 65 % of that gap — with the primary margin at +0.191 [+0.137, +0.247] and unsafe actions at 0.26 % against 0.92 %; but **seed 18 reaches `grasp` 0.876 and +0.247 in one epoch**, so the epoch effect and the seed effect are the same size and the two-epoch column was run at one seed only. Two things this round is blunt about. **`q_stop` does not replicate at all** — the three seeds react to **1, 8 and 0** of the cell's 10 stop onsets under an identical recipe, so on this corpus the stop detector is seed noise, and at 466 steps every shuffled control catches *more* onsets than the model does, meaning that answer depends on neither the instruction nor the goal state. And **the two-epoch run is not the continuation it was launched as**: `adapt_readout.py` was passing the config to `Trainer` without `resume=`, so the `resume:` key validated and was then silently dropped, and what ran was 466 steps from scratch under one schedule — a clean "one epoch versus two, each trained to the end of its own schedule" comparison, but ≈4.0 GPU-hours more than the continuation would have cost, and **the documented resume-and-reschedule path still has no GPU run behind it**, only CPU tests. **Task R4 (2026-09-24) put the seed-18 checkpoint in the policy seat of the simulator loop and ran the first closed-loop set — dev 100 and ood_dev 26 new seeds, five policies on the same seeds.** The loop-side path (incremental ts0.6 serializer, static prefix KV + 30-tick window, ten decision branches in one fused forward, batched pointer readout) reproduces the offline evaluation **bit-for-bit** on three recorded dev episodes (3,890 answers, probability difference 0) and runs at **p50 43.6 / p95 56.8 / p99 82.7 ms per tick with trained weights, 0 of 7,171 ticks over 100 ms**. Success rates (seed-level 95 % intervals): expert 0.970 [0.930, 1.000] / 1.000, rule judge 0.800 [0.720, 0.870] / 0.692 [0.500, 0.846], mechanical 0 / 0, **model 0 / 0** (the two-epoch checkpoint 0.020 [0, 0.050] / 0 — one of its two `done` episodes is a scene the expert and rule judge also complete without a close, the other a false done with the target outside its zone; `done ∧ target_inside_zone` 1/100, and the expert and rule judge have no false dones); paired rule − model **+0.800 [+0.720, +0.870]**. The model reads the instruction in the loop as it did offline — its adopted action matches the expert reference on 91.8 % of decisive ticks and follows a goal change on the tick it happens 95.9 % of the time (rule judge 79.9 %) — and **never initiates the gripper close** (0 of 125 reference transitions), so every episode hovers at the grasp point until the stall guard ends it. The same checkpoint scores `q_gripper` 0.996 offline: the recorded corpus asks "close now" as a single-valued label on 9 of 23,192 training ticks (the rest are already-closed ticks the model answers by copying the executed state, and ±1-tick tolerance windows where it predicts `closed` on 1 of 271 — the tolerance makes 307 of the 316 wanted-closed-while-open training ticks two-valued). The rule judge saturates the no-instruction-change layer (equal to the expert) and not the instruction-change layer (−0.213 [−0.312, −0.125]), so docs/06's task-redesign trigger is not met. The first run report is `docs/reports/run-report-1.md`; its recommendation, from the numbers: **do not scale to the cloud yet — repair the auxiliary-judgment labels and evaluation cell first.** GPU 0.65 h. See [Status](#status) for what is and is not verified.

## Why

Robot task intelligence today lives in large vision-language models that plan in seconds. Execution lives in controllers that run at hundreds of hertz but understand nothing. Between them is a gap: the moment when a grasp fails, an instruction changes, an object moves, or a person steps in — decisions that need *meaning* (what the instruction wants, what is forbidden, what has already been tried) at the speed of a reflex, not of a paragraph.

robojev is that layer: **semantic System One at 10 Hz.** It does not reason in chains of thought and it does not compute torques. It reads the situation and returns typed judgments that a deterministic executor can act on immediately.

```text
L3  task intelligence  (VLM / planner, seconds)     goals, constraints, plans
        ↓
L2  robojev            (10 Hz stream, typed)        action commitment · stop · gripper · path · speed · force
        ↓
L1  executor           (50 Hz, scripted / RL)       blending, leases, reflexes, gripper events
        ↓
L0  physics / motors   (500 Hz)
```

## Core ideas

- **The harness asks, the model judges.** A harness turns the world into a structured state (objects with tracked ids, poses, precision, visibility; zones; robot state; executed history) and enumerates *candidate* joint actions (`grasp o3 → zone L`, `push o5 −x`, …) with geometry it computed itself. The model never generates candidates or text; it scores them. Anything a classical computation can answer exactly (reachability, clearance, distances) is given to the model, not asked of it.
- **Ten typed questions per tick, answered in parallel.** The main decision is a choice over joint-action candidates (K ≤ 32); four gating booleans (done, instruction sufficient, observe more, retry ok); stop; desired gripper state; path (direct / via waypoint / retreat / hold); speed and force as ordinals. Auxiliary answers are conditioned on the commitment fixed at the start of the tick, so the model is never asked "which speed" for an action it might switch away from in the same tick.
- **Pointer readout, not classification.** Every decision position reads all candidates through a shared bilinear readout `z_ik = (U h_d)ᵀ (V h_c) / √r + b`, so candidates can change meaning and count on every request and the same weights serve robot ticks and general single-request judgments alike. Candidate-branch readout (one path per candidate) is kept only as a comparison group.
- **Streaming state.** A robot episode is one append-only token stream: a static prefix, then per tick `[state][executed history][dynamic candidates]`, from whose end the ten decision positions branch as *transient* one-token forks. The next tick continues from the pre-branch state. Full-attention layers see the static prefix plus a 30-tick window; linear-attention (DeltaNet) layers carry the whole episode. Training uses truncated BPTT over 10-second chunks so the model trains on exactly the state it will infer with.
- **Composition rules and a real executor contract.** Validity checks, stop-first ordering, gating, hysteresis on the main decision (switch only when the same challenger beats the incumbent by δ for m ticks), auxiliary-answer application, and command generation are deterministic and versioned. The controller enforces observation deadlines, geometry-age tolerances, issue leases, blending, stop transitions, reflexes and idempotent gripper events, and every acknowledgement is recorded exactly as executed.
- **Information boundary.** Nothing the front end could not know reaches the model: occluded objects keep their last observed pose, simulator events never leak, labels and evidence live outside the input area and a validator rejects any request that carries them. The executed history is never replaced by a label, in expert episodes or in DAgger relabeling.
- **Labels from evidence, not decree.** Main-decision labels come from semantic admissibility (instruction, destination, forbidden contacts) → measured outcomes of counterfactual rollouts at keyframes (paired seeds, censoring preserved) → a commitment rule. Candidates that were not rolled out are `unknown` and trained with a partial-label loss. A rule-based judge and a tiny from-scratch scorer serve as baselines so that no result is credited to "understanding" that pattern matching would also achieve.

## What is built

| Area | Modules | Verified by |
| --- | --- | --- |
| Contracts | `contracts.py` — request/label schema, validation, `model_input` boundary | contract and boundary tests; D0 fixtures (64 single requests, 4 synthetic streams) |
| Non-robot data | `data/generate.py`, `domains.py`, `split.py`, `validate.py` — four domains, origin-group splits, automatic QA | 2,000 regenerated records, zero QA violations |
| Simulator and executor | `sim/environment.py` (MuJoCo / robosuite, E0/E1 scenes, disturbances, instruction changes, snapshot/restore), `sim/controller.py` (executor contract) | E0 closed loop completes with the rule baseline alone |
| Streaming harness | `harness/robot.py` (candidates, pruning, waypoint planner, composition v0, commands), `harness/rule_judge.py`, `perception/pointworld.py` | regression tests for every defect found by external review; E0 seeds complete |
| Expert and data pipeline | `sim/expert.py`, `data/robot_episodes.py`, `sim/label.py`, `data/rollouts.py`, `data/dagger.py`, `configs/sim/events.yaml` | E0 and E1 episodes complete end to end; first 100 rollouts costed (0.345 s each → 128k ≈ 12 CPU-hours) |
| Model (CPU reference) | `model/serialize.py` (real tokenizer, two layouts), `model/attention.py`, `model/hybrid.py` (differentiable DeltaNet/conv state fork, tiny hybrid), `model/stream.py`, `model/judge.py`, `loss.py` | incremental == from-scratch computation in FP32 and float64; branch isolation bitwise; the plan's mandated tests verbatim |
| Training (CPU reference) | `train.py`, `sampler.py`, `checkpoint.py` — TBPTT, mixed sampler, atomic checkpoints, identity-checked resume | 20 continuous steps == 10 + process restart + 10, bit-identical |
| Run launcher | `launch/manifest.py` (run manifest + budget arithmetic), `launch/runner.py` (the remote side, which enforces the caps itself), `launch/launcher.py`, `launch/providers/{base,ssh,local}.py`, `scripts/launch_run.py`, `infra/run-manifest.schema.json` | a localhost round trip over real SSH (`prepare → launch → status → fetch`, five artifacts, every sha256 matched) and four deliberate failures — wall-clock cap, USD cap, `kill -9`, an unconfirmed `cancel` (which leaves `unknown`, not `cancelled`) |

`uv run pytest -q` → **1,276 tests** on a full checkout (`uv sync --group backbone`). A CPU-only checkout without that group reports **1,255**: `tests/test_backbone_qwen.py`'s 22 tests collapse into a single module-level skip, `test_evaluate.py:188` skips too, and one, `test_measure_candidates.py::test_stream_runner_loads_the_checkpoint_before_compiling_so_lora_keys_survive`, fails because it reaches the backbone through `QwenBackbone.tiny` with no guard. 1,230 = 1,209 − 1 module skip + 22.

## Status

What is verified and what is not, per area. Nothing below the line is claimed.

| | Verified | Not yet |
| --- | --- | --- |
| Robot closed loop | E0 (static scenes) with rule judge and scripted expert; E1 (instruction change + disturbance) on 3 seeds | E1 at scale; real-robot front end (3D reconstruction) |
| Data | **D1 first version (2026-09-20, Task D1):** robot 400 episodes (`artifacts/datasets/d1-robot/d1`: 36,690 ticks — episodes end at done + 1 s, mean 9.2 s, so the plan's 120K-tick assumption was an upper bound; E0 199/200 · E1 196/200 completed; OOD 58/400 = 14.5 %; 990 tick contrast pairs; QA 0 violations / 0 leaks) + non-robot 2,000 records (951 base states, 760 contrast pairs, QA 0/0); 100,640 keyframe rollouts (2,000 keyframes × ≤ 8 candidates × 8 seeds; success 64.5 %, censored 0.90 % all `candidate_unavailable`; place 93.8 % / grasp 50.9 % / push 48.9 %; 0.51 s/rollout, ≈14 CPU-h, 2 h wall at 8 workers; resumable per episode chunk) attached as the lineage version `d1-rollout-labels` (`labels-rollout-v1`: 2,000 `q_main` labels, high 1,806 / low 194); carried fixes h0.7 (stall-guard absolute cap, push stand-off slack with a reproducible A/B driver, `zone_full` record); stratified review sample of 900 questions = 870 open (29 core strata × 30, `sample.*`) + 30 sealed `ood_test` rows in separate `sealed.*` files for a non-developer reviewer (120 double-review) with the protocol; D-OOD config stubs; Task 2c tiny-scorer baseline table (`artifacts/reports/tiny-scorer.json`); BoolQ/MultiNLI converted as evaluation-only public sets. Earlier: place-phase fix (h0.5/e0.4/c0.6: E1 sweep 23/26 → 26/26), contrast siblings with a deletion QA gate, pre-128k sweep on batch-0 (9,880 rollouts), sealed holdouts with a zero-leakage QA check | human review of the D1 sample; D-OOD generation (100 episodes + 5,000 states); D2 scale |
| Model | contracts, masks, state forking, readout and losses on a random-weight fixture; the real stream path on Qwen3.5-2B/4B (static prefix KV + 30-tick window buffer, DeltaNet state carried through fla, mask-free varlen flash attention, batched decision branches, pointer readout) with a measured BF16 tolerance, branch isolation and next-tick invariants; **everything in this row up to the Task P3 block below was measured on the OLD population — 8 of the `ood_dev` split's 24 episodes, 844 ticks, with the donor rotation that 8-episode list implies. It is kept, not deleted, and scoped.** **The D1 pilot (2026-09-21, Task P1): seven runs at the same data, steps and seed on one fixed evaluation set (`configs/eval/pilot.yaml`; the five adapted runs share `eval_set.sha256 79d09793eab5`, the two zero-shot runs score every 16th tick and carry `c772c669c7be`). On the decision cell — `robot/ood_dev q_main`, 844 ticks from **8 episodes** of 94/80/67/79/72/70/**300**/82 ticks, so one episode is 35.5 % of it — every accuracy is read against **that run's own** state-shuffle control: 2B LoRA (40) 0.570 (0.402) = **+0.168**, 2B T0 (200) 0.454 (0.289) = **+0.165**, 2B T1 (40, full text backbone) 0.712 (0.601) = **+0.111**, 4B LoRA (40) 0.642 (0.544) = **+0.098**, 4B T0 (200) 0.871 (0.799) = **+0.072**. The highest raw row has the smallest control-adjusted margin, the 4B T0 scores 0.799 with the goal and the whole state replaced (rule judge 0.821 on this subset, **0.731** on the whole split), and every run is within ±0.014 of its own instruction-shuffle control — so no adapted model demonstrates that it reads the goal at this scale. **Task P2 (2026-09-21) measured that interval and re-ran the T1 with the update fixed.** The table now keeps a per-episode aggregate in every question cell and a **paired bootstrap over episodes** gives the interval of the margin itself. Re-evaluating each P1 checkpoint on the same 8 episodes / 844 ticks reproduces its published numbers **bit-identically** (all 20 values — model / state shuffle / instruction shuffle / rule judge × the five re-run rows — at Δ = 0.00e+00, n = 844 each), so the intervals attach to them: 2B LoRA +0.168 [+0.131, +0.225], 2B T0 +0.165 [+0.111, +0.249], 2B T1 +0.111 [+0.066, +0.161], 4B LoRA +0.098 [+0.060, +0.128], and **4B T0 +0.072 [−0.015, +0.134] — the only one that contains zero.** The row with the highest raw accuracy is the one whose margin is not distinguishable from noise. Every instruction-shuffle margin contains zero. A 2B T1 re-run with **fp32 master weights** (same data, steps, seed and eval set) reads **0.994** on the decision cell — but its own state-shuffle control reads **0.904**, 0.083 *above* the goal-reading rule judge (0.821), and its margin over the instruction-shuffle control is **−0.001 [−0.002, +0.000]**. A separate bf16 run at the same 5 s chunk splits the +0.282 gain into +0.064 from the chunk and +0.218 from the masters (a decision-cell-specific split: on `robot/dev q_main` the same chunk control goes 0.880 → 0.792 and on `robot/test q_main` 0.890 → 0.806). **Why the control scores 0.904 (measured in the 2026-09-21 fix round): the cell is ~70 % 'repeat your commitment'.** On **595 of the 844 ticks (70.5 %)** the expert label *is* that tick's own `commitment.action_ref` — 98.5 % of the 604 ticks that carry one — and the state shuffle keeps the commitment line verbatim, so a policy that reads nothing but the preserved fields scores **751/844 = 0.890**, within 0.014 of the model's shuffled 0.904; the shuffled T1 in fact repeats that commitment on 97.0 % of the ticks that have one, and 76.3 % of its correct answers are that repetition. **Stratified, the reading flips.** On the **249 ticks whose label is not the commitment** the fp32 T1 scores **0.984 against its control's 0.727 — a margin of +0.257 [+0.073, +0.318]** that does not contain zero, while the 4B T0 scores 0.863 against its control's 0.904 (**−0.040 [−0.157, +0.125]**, which does contain zero, so it is not a finding either way). Across all seven re-evaluated runs that stratum has **exactly one** margin that excludes zero — the fp32 T1's — while every run's margin on the 595 commitment ticks excludes zero (+0.020 to +0.230), which is what a stratum whose answer the control copies through should look like. So **the aggregate margins are diluted by a stratum that needs no goal**, P1's verdict stands for the seven runs P1 measured, and the properly-trained T1 is the exception — with two limits: 159 of those 249 ticks are the same single episode, and both strata still match their instruction-shuffle control, so the *goal text* is still unread. Details: `artifacts/reports/p2-decision-cell-strata.json`. **Task P3 (2026-09-22) rebuilt the instrument before the task is redesigned, and the headline moved.** The evaluation population is now **every episode of the `ood_dev` split, taken whole — 24 episodes / 2,530 ticks** (`configs/eval/p3-decision-cell.yaml`, `eval_set.sha256 bf80f9a54063`), against the 8 episodes / 844 ticks P1 and P2 used; ticks are not thinned, because a stream is replayed from tick 0 so thinning saves no GPU and truncating the tail would delete 251 of the 525 reading-stratum ticks. The **primary metric is pinned in `docs/08` §10**: the ticks whose expert label is **not** that tick's own `commitment.action_ref` (525 of 2,530; the other 2,005 are "repeat your commitment", where the control copies the answer through and a **mechanical baseline** that reads nothing scores 1.000 by construction — 0.875 over the whole cell, 0.398 on the reading stratum). Three things came out of re-measuring all seven checkpoints there. **(1) The fp32 T1's margin survives but is a third the size**: +0.080 [+0.022, +0.165] (episode-balanced +0.100 [+0.074, +0.133]) against P2's +0.257 [+0.073, +0.318], and it excludes zero under every leave-one-episode-out refit. **(2) Most of P2's number was the instrument, not the model.** The state-shuffle control takes its donor from the next record in the config's list and clamps to the donor's last tick when the donor is shorter — in the 8-episode rotation `ep-E1-000235` (300 ticks) drew an 82-tick donor, so 218 of its ticks, including all 157 `observe` ticks, were shuffled against one frozen finished state. On the same 249 ticks with the 24-episode rotation the same checkpoint reads +0.024 instead of +0.257; the model column is identical on all 844 shared ticks, the state-shuffle column differs on 9.7 %. **(3) The margin is one question family.** Splitting the reading stratum by the key of its answer, every adapted run beats its goal-blind control on the **61 `grasp` ticks** (fp32 T1 +0.656 [+0.552, +0.840]; 2B T0 +0.197; 2B T1 +0.164; bf16-5 s +0.230; 2B LoRA +0.262; 4B T0 +0.361) where the rule judge that reads the goal scores 1.000 and the mechanical baseline 0.000 — and **no run beats it on the 210 observation-gate ticks**. The `hold` done-tail (251 ticks) is answered from the execution state by both columns. With 24 episodes instead of 8 the interval also resolves what the old cell could not: four rows now exclude zero on the reading stratum where only one did, so "only the fixed run reads the goal" was a statement about the old cell's resolution. Every instruction-shuffle margin still contains zero in every stratum. Details: `artifacts/reports/p3-population.json`, `p3-decision-cell-strata.json`, `.superpowers/sdd/task-p3-report.md`. Zero-shot label scoring is better on the 4B on every split (robot dev 0.776 vs 0.484, stride 16). No adapted run beats its own zero-shot on the contrast `choice` type, and over 20 contrast pairs per split each run changes its `q_main` answer on 0–2 pairs of 20 and 0–1 of the 6 instruction pairs, never with both members correct** | a backbone trained at scale; a task whose decision cell is not answerable with the goal and the state replaced; calibration |
| Training | step, TBPTT, sampler, checkpoint, resume on the fixture; on the real 2B/4B: readout-only, peft-LoRA **and full text-backbone (T1)** runs on Spark with candidate-order permutation augmentation, per-layer activation checkpointing and a unified-memory guard. **Task 5's GPU acceptance is done (2026-09-21): T0 leaves all 320 backbone tensors bit-identical and saves only the readout; LoRA and T1 put non-zero gradients on every trainable tensor and move them; 20 steps continuous vs 10 + process restart + 10 is bit-identical (loss, sampler position, drawn records, optimizer step, parameters) → G0 passes. Measured cost at D1 scale: 2B T0 200 steps 9.9 s/step / 10.1 GiB / 4,664 tok/s, 4B T0 22.1 s/step / 20.8 GiB, 2B LoRA 63.0 s/step / 26.8 GiB, 2B T1 57.1 s/step / 60.0 GiB, 4B LoRA 131.9 s/step / 64.8 GiB.** **fp32 master weights are in (2026-09-21, Task P2)**: the optimizer keeps an fp32 master of every trainable parameter that is not already fp32 — so the readout and LoRA are untouched and only T1 changes — steps in fp32 and writes back to BF16. A pass/fail test pair fixed before the measurement pins it (update-size ratio **0.9766** with masters, **0.0000** without, tolerance 0.05). Cost on the 2B: **+14.03 GiB** resident during the backward (predicted +14.02) and **+0.2 %** per step, so the T1 chunk moved 10 s → 5 s to stay inside the 73 GiB fence; a 4B T1 does not fit at any chunk (optimizer-step floor 94.0 GiB). What it recovers, on the same 24-tensor sample after 40 steps at `backbone_lr 1e-5`: elements moved outside the embedding **23.32 % → 65.70 %**, mean |Δ| over all of them **1.93 % → 8.49 %** of the unrounded expectation — **4.4×** the drift the BF16 grid was throwing away. Also fixed: a T1 checkpoint could be **saved but not loaded back** (the tied `lm_head` ↔ embedding weight is stored under one name and the loader counted the other as missing). **The resume gate is now wired to the scope it gates (2026-09-21 fix round)**: tolerances are per training scope (`RESUME_TOLERANCES`, and only `t0` is pre-registered), a scope without one stops at `passed: null` instead of being judged by another scope's tolerance, and a launcher reads the key for the mode it is about to start (`p1_acceptance.py --gate t0|lora|t1`) — before this, the only automated gate read `checks.resume`, the T0 key, so long T1 runs were gated by the T0 result. The pre-P2 `fp32_master_weights` key also no longer refuses T0/LoRA checkpoints where it is a no-op | cloud-scale training; a **pre-registered** T1 resume tolerance from a two-run baseline; calibration |
| Latency | token counts with the real tokenizer under contract v0.3 (10 objects, K=12: p50 332 / p95 599 / first tick 880 tokens; 37K per 10-s chunk); native-path screen on DGX Spark (2026-09-19; 2B/4B/9B/27B p95 928 / 2,299 / 2,447 / 5,640 ms at the old format, 123 / 305 / 378 / 886 ms at a 500-token tick; v0.3 re-run 2B p95 97–103 ms on a growing cache); **the real stream path (2026-09-20, `scripts/measure_candidates.py --path stream`, `artifacts/reports/backbone-stream{,-levers}.json`): 2B baseline p95 74.7 / 83.8 / 82.3 ms (lower / upper / instruction change) and 80.0 ms on 40 batch-0 episodes — 3.8 ms over on `upper`; with the `fused` lever, re-measured on all 40 episodes (`backbone-stream-fused.json`): 62.7 / 60.2 ms (p99 91.1 / 84.7, max 91.1 / 101.0), 2 of 1,348 ticks over 100 ms (0.15 %, both E1 tick-60 intro ticks of ≈1,000 tokens) → passes 10 Hz with 17 / 20 ms of margin; `all` = fused + dense compile + bf16 readout (the default serving configuration): 52.5 / 52.0 ms (max 75.3 / 83.1), 0 misses; the 8-episode lever screen had read 65.0 / 57.3 on an E0-only subset. 4B 182 / 176 ms baseline, 144.6 / 122.6 ms fused (8 episodes) → 5 Hz only. Cache window-sized by construction, memory constant across ticks (2B peak 4.14 GiB). Attribution: the per-request intercept is weight-read-bound (CUDA graphs remove only 3–5 ms), so FP8-9B stays closed** | the same gate on the robot with the real front end; a trained checkpoint's latency (identical by construction — the readout is value-independent) |
| Cloud and cost | **the launcher, end to end on this box and for $0 (2026-09-23, Task R3b)**: a run manifest (`run-manifest-v0`) that pins the code revision (git sha **and dirty flag**), the training config and its sha256, every dataset manifest's path and sha256, the tokenizer and contract digests, the seed, `max_steps`, the checkpoint policy, the three caps **and the hourly price**, the artifact list, the state and the ledger of transitions; three caps folded into one wall-clock deadline (`max_usd / hourly_usd`, `max_gpu_hours / gpus`) that the **remote runner** measures, so it holds when the launcher dies; `prepare` **refuses a run no cap can stop** (and refuses `--max-usd` with a zero price) unless `--no-cap` asks for it by name and the manifest records that; a `resume` takes the parent's spend from the run's own fetched `state.json` and refuses when no spend was ever observed; `fetch` re-hashes what it brought back against what the remote measured; `cancel` leaves **`unknown`** when it cannot confirm the stop. Acceptance: a localhost SSH round trip on `tiny_cpu.yaml` (5 steps, **145.688 s** by the runner's own clock — the trainer's `metrics.json` says 144.85 s) and four deliberate failures, each leaving a checkpoint and a log that `resume` continues from | anything on a real provider: no instance has ever been created. `Provider.create/destroy/describe` is an **interface only**, so the GPU-shutdown half of Task 6's failure drill is not done. The provider, the account, the price and the approval for the first paid run are the user's decisions; the H100-to-GB10 throughput ratio behind every cost figure is **assumed, not measured** |

Three measurements changed the plan; all three are now decided:

1. **Token budget (decided 2026-09-19, contract v0.3).** With the old serialization a tick cost 1,764–3,712 tokens (estimate was 450–700). Contract v0.3 — short field names, an object intro/dynamic split with delta ticks (intro every 30 ticks, dynamic on change or every 10), zones and scene in the prefix, a compact goal reference and key-based candidate lines — measures p50 332 / p95 599 / first tick 880 tokens (1,331 with the uncacheable 451-token episode prefix ≈ 80 ms on the 2B) at 10 objects and K=12 (37K per 10-s chunk; the regenerated batch-0 reads p50 393 / p95 566), inside the 500 / 800 / 1,200 / 60K budget. Details: docs/08 §3.
2. **Candidate cap (decided 2026-09-19, contract v0.3).** The speed profile left the joint key (`q_speed` answers speed), pushes are enumerated only toward the zones, the cap is K ≤ 12 (9 + 3 reserved) and the instructed target×zone combination is reserved before the round-robin spread (it reads the instruction, never the label). On the E1 sweep (seeds 1–24, 29, 43) the degenerate-`hold` rate went from 49.7% to 0% and completion from 20/26 to 23/26 (like-for-like on the 22 seeds whose plan the A6 scene fix did not change: 16/22 → 19/22; the 4 changed seeds were 4/4 before — one trivially at tick 0 — and 4/4 after). Non-keyframe ticks now carry a planner-cost allowed set (τ = 0.15) and executor-caused `hold` ticks follow a hold∉A rule; sealed holdouts (docs/04 §5) went in with the same change.
3. **Backbone (decided 2026-09-19, final 2026-09-20).** The Spark screen fits per-request model time as an intercept plus a token cost — 26.5 / 51.6 / 93.6 ms + 38.6 / 107.7 / 152 ms per 1K new tokens for 2B / 4B / 9B — so the 9B is 3.5–4.7× over budget and left the 10 Hz track; the 27B is excluded. Stage 2 then measured the real stream path: **Qwen3.5-2B with the `fused` tick passes the 10 Hz gate on all 40 batch-0 episodes (p95 62.7 ms `upper` / 60.2 ms batch-0, 2 of 1,348 ticks over 100 ms; `all` 52.5 / 52.0 ms, 0 misses; the baseline missed `upper` by 3.8 ms), the 4B passes only 5 Hz (144.6 / 122.6 ms fused)**; the attribution measurement shows the intercept is weight-read-bound (CUDA graphs save 3–5 ms), which closes FP8-9B. Quality: the 4B's zero-shot judgment is clearly better (75.7 vs 48.0 % on the robot dev, 51.8 vs 42.1 % on the pilot dev), the readout-only 2B reaches 88.3 % on the (tiny, 5-episode) robot dev and 46.5 % on the pilot dev, ≈5 pt over the context-shuffle control; the 2B LoRA (r=16, 40 steps, loss 2.05 → 0.52, 59 s/step, peak 26.9 GiB) reaches 86.5 / 84.6 % on robot dev / ood_dev (shuffle control 87.1 / 84.6) and 45.4 / 44.3 % on pilot dev / ood_dev (shuffle control 41.2 / 38.3; answer-change 24.8 %); the 4B LoRA (r=16, 30 steps, loss 2.68 → 0.64, 120 s/step, peak 64.8 GiB) reaches 75.7 / 75.2 % on robot dev / ood_dev (shuffle control 76.5 / 75.2) and 41.4 / 42.9 % on pilot dev / ood_dev (shuffle control 35.4 / 35.7; answer-change 37.3 %); the latency gate is binding, so the 2B is the backbone and the 4B the 5 Hz fallback. The D1 pilot (Task P1) then measured the two like-for-like and did **not** settle quality: read against its own state-shuffle control the 4B T0's margin on the decision cell is the smallest of the five adapted rows (+0.072) and no run beats its instruction-shuffle control, so the pilot gives no quality reason to move to 5 Hz. Task P2 put an episode-clustered interval on those margins and the 4B T0's **+0.072 is the only one that contains zero** ([−0.015, +0.134]), so the 5 Hz question is not reopened by a quality difference that can be measured. Full numbers: `artifacts/reports/backbone-stream.json`, `backbone-selection.json`, docs/03, docs/06 Task 2b.

## Repository

```text
docs/        research design (01–06), external reviews (07, 09, 10, 11), and the canonical stream contract (08)
src/robo_jev/
  contracts.py  data/  sim/  harness/  perception/      # world, harness, expert, data pipeline
  model/  loss.py  sampler.py  train.py  checkpoint.py   # model contracts and CPU training path
  launch/       run manifest, budget caps, remote runner, ssh/local backends   # send a run to another box
configs/     harness, controller, simulator, expert, events, data, model fixture, training
infra/       run-manifest.schema.json — what a run must pin before it is allowed to cost money
scripts/     generate_episodes · rollout_keyframes · dagger_cycle · measure_tokens · fetch_tokenizer · fetch_backbone · measure_candidates · adapt_readout · p1_acceptance · attribution · select_backbone · launch_run
tests/       1,276 tests (1,255 without the backbone group); fixtures under tests/fixtures
HANDOFF.md   how to continue on another machine; what lives outside git
```

`docs/08-streaming-io-and-data-contract.md` is the source of truth for the robot stream. Where any other document disagrees with it, 08 wins.

## Quick start

```bash
uv sync                                   # Python 3.11; MuJoCo + robosuite 1.5, torch (CPU on macOS)
uv run python scripts/fetch_tokenizer.py  # Qwen tokenizer.json into artifacts/ (pinned by manifest)
uv run pytest -q                          # 1,276 tests (tokenizer present; 1,255 without `--group backbone`)

uv run python scripts/generate_episodes.py --config configs/data/d1_robot.yaml --count 4 --out artifacts/datasets/d1-robot/smoke
uv run python scripts/rollout_keyframes.py --limit 20
uv run python -m robo_jev.train --config configs/train/tiny_cpu.yaml

# send a run to another box, with caps, and bring it back (ssh + rsync; `--backend local` runs the same code here)
uv run python scripts/launch_run.py prepare --config configs/train/tiny_cpu.yaml --run-id demo \
    --manifest artifacts/scratch/demo/run.json --backend ssh --host <host> --user <user> \
    --remote-dir /mnt/persist/demo --remote-repo /mnt/persist/robo-jev --remote-python 'uv run python' \
    --gpus 1 --hourly-usd 2.50 --max-wall-hours 6 --max-usd 12.5
uv run python scripts/launch_run.py launch --manifest artifacts/scratch/demo/run.json
uv run python scripts/launch_run.py status --manifest artifacts/scratch/demo/run.json --wait
uv run python scripts/launch_run.py fetch  --manifest artifacts/scratch/demo/run.json --dest artifacts/runs/demo
```

## Roadmap

1. Decide contract v0.3 (compact serialization, delta ticks, candidate space, allowed-set labels), seal semantic holdouts, regenerate the first batch, re-measure tokens.
2. ~~Measure candidate backbones (2–4B, 9B) on deployment-class hardware with representative inputs~~ — done (native screen 2026-09-19; real stream path, readout-only/LoRA/zero-shot quality and the selection 2026-09-20; see Status).
3. ~~First trained model: readout-only and LoRA on the pilot fit on the Spark itself~~ — done (2026-09-21, Task P1: 2B/4B × T0, LoRA, zero-shot plus a 2B full text-backbone T1, **9.22 GPU-hours counted** (553.5 min of step windows; 9.36 h including the 8.5 min aborted stride-8 zero-shot attempt) on the Spark; see Status). ~~a T1 with fp32 master weights~~ — done too (2026-09-21, Task P2; see Status). The cloud is for D2 scale, seeds and 4B full training, which does not fit here at all under fp32 masters. Calibration is still open (the pilot reports uncalibrated ECE only).
4. ~~A launcher that makes a run specified, capped and auditable, so the same run can be sent to another box and brought back~~ — done (2026-09-23, Task R3b: `scripts/launch_run.py`, `infra/run-manifest.schema.json`, a localhost SSH round trip and four deliberate failures, **$0 spent**). What is left is not code: the provider, the account and the approval for the first paid run.
5. Full D1 rollouts, DAgger cycles, closed-loop comparison against the rule baseline with decision-stability metrics.

## Acknowledgements

The design follows the public description of TypeSafe's Jev / System One models (structured state in, typed probabilistic judgments out, one pass). The open minimal option scorers (jevlike, cua-s1) informed the tiny baseline and the shuffled-context control. robosuite and MuJoCo provide the simulator.
