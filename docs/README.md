# 병렬 판단 모델 연구 문서

이 폴더에서 모델 구조, 데이터 생성, 실제 학습과 평가 계획을 정리한다. [개발 계획서 v2](../Steerix_Robotics_Jev_Development_Plan_v2.docx)를 출발 자료로 삼으며 현재 연구 방향은 아래 문서에 반영한다.

**하네스가 상태·분야별 질문·타입·후보를 구성하고, 모델은 여러 질문의 선택·확률·점수를 병렬로 반환한다. 하네스가 답을 조합해 실행한다.** 모델이 질문과 후보를 생성하는 역할까지 맡지는 않는다.

직접 개발할 범위는 모델 구조, 학습 데이터 생성 파이프라인, 실제 학습·체크포인트, 판단 평가와 로봇 폐루프 검증 전체다. 로봇은 첫 응용이며 질문 의미와 후보를 특정 행동 목록에 고정하지 않는다.

## 현재 문서

| 문서 | 내용 | 상태 |
| --- | --- | --- |
| [연구 컨셉과 목표](01-research-concept-and-goals.md) | 병렬 판단 모델과 하네스의 책임, 입출력, 연구 목표와 비교 원칙 | 연구 설계 초안 |
| [첫 로봇 하네스와 학습 문제](02-task-and-learning-problem.md) | 대표 작업, 질문 묶음, 답의 조합·실행, 데이터 생성과 실제 학습 | 단일 팔·평행 그리퍼를 전제로 한 초안 |
| [모델 구조와 학습 설계](03-model-and-training-design.md) | backbone 후보, 공유 prefix·질문 분기·후보 readout, 손실, 실제 학습 단계와 기준군 | 구현 전 설계 |
| [데이터 생성 계획](04-data-generation-plan.md) | 다분야 질문 묶음, 라벨 근거, 시뮬레이션 결과, split·QA, 5K→50K와 반복 수집 | 제작 계획 |
| [실험 환경·GPU 클라우드·비용](05-experiment-and-cloud-plan.md) | 시뮬레이터, 학습 환경, GPU 메모리·가격·비용, checkpoint·평가 | 공개 가격 확인, 자원 미생성 |
| [첫 학습 파이프라인 실행 계획](06-execution-roadmap.md) | 파일·인터페이스·인수 검사, 최초 학습까지의 순서, 12주 산출물과 판단 기준 | 구현 대기 |
| [설계 변경 검토](07-design-change-review.md) | 다섯 가지 변경에 대한 외부 검토(R1~R6)와 공개 근거 해석 정정 | 검토 결과, R1~R6 반영 완료 |
| [스트리밍 입출력·데이터 계약](08-streaming-io-and-data-contract.md) | robojev의 계층(L2), 10Hz 스트림 입력, 9개 질문 세트, 조합 규칙, 컨트롤러 계약, 라벨·레코드 정의, 01~06 반영 목록 | 설계 합의안(리뷰 09 반영). 로봇 스트림의 정본이며 01~06에 반영 완료 |
| [스트리밍 계약 검토](09-streaming-contract-review.md) | 08에 대한 외부 검토(S1~S7)와 수치 검산 | 검토 결과, 08에 반영 완료 |

## 현재 실행 방향

1. 요청·라벨 계약과 수동 검수한 64상태를 먼저 만든다. 로봇 상태에는 결합 행동 후보의 주 결정을 포함한다.
2. 로봇 판단 주기의 지연 예산에서 backbone 규모의 상한을 역산하고, 후보(첫 후보 Qwen3.8-27B, 소형 dense·MoE·full-attention 후보)의 연속 틱 지연과 무학습 품질을 실측해 본 실험 backbone을 확정한다.
3. D1(비로봇 단일 요청 2,000 상태 + 로봇 스트림 400 에피소드, 스크립트 전문가·규칙·키프레임 rollout 라벨)을 생성하고, 선정한 backbone으로 실제 학습(스트림은 truncated BPTT)·저장·재개를 검증한다. readout은 결정 위치 pointer 방식이며 후보별 분기 readout은 추가 연산을 쓰는 비교군이다.
4. 같은 backbone에서 50K 데이터·상태 공유 구조·여러 seed의 비교로 확대한다. 일반 attention과 DeltaNet 양쪽의 분기 격리·gradient를 검증하고, 상태 선행·질문 선행 배치를 비교한다.
5. 단일 상태의 여러 질문과 연속 틱의 품질·지연을 확인하고, 규칙 기반 판단기 기준군과 결정 안정성 지표를 포함한 고정 로봇 하네스의 폐루프 성과와 연결한다.

모델·시뮬레이터·클라우드 구성은 제안이며 장비·계정·예산은 아직 확정하지 않았다. 현재 완료한 것은 연구·구현 계획 문서다. 데이터 생성, 학습 코드 구현, GPU 프로비저닝과 학습 실행의 완료 여부는 별도로 기록한다.

모델 선정은 2026-09-18 공식 최신 계열 확인 후 수정했다. 기존 Qwen3-14B→32B 기본안은 사용하지 않는다. Qwen3.8-27B는 첫 후보이자 품질 기준으로 유지하되, 본 실험 backbone은 지연 예산의 실측 뒤 확정한다. 선정 절차, 후보, hybrid 구조의 구현 조건은 [모델 설계](03-model-and-training-design.md)에 기록한다.

## 설계 변경 기록

2026-09-18 근본 검토(Jev 핵심 컨셉의 재현 가능성과 로봇 실시간 행동 결정 적합성)를 반영해 다음을 바꿨다.

| 변경 | 이유 | 반영 문서 |
| --- | --- | --- |
| readout을 후보별 분기(`Q×K`)에서 결정 위치 pointer(`Q`)로 변경. 후보별 분기는 추가 연산을 쓰는 비교군 | 후보별 backbone 재실행과 순차 출력 생성을 없애 한 경로에서 후보 분포를 읽고 학습·추론 토큰을 줄이기 위해. 후보 토큰 인코딩·점수 계산 비용은 남음 | [03](03-model-and-training-design.md), [06](06-execution-roadmap.md) |
| backbone을 판단 주기의 지연 예산에서 역산해 선정. 규모 고정은 비교군 사이의 원칙으로 유지 | 27B dense·단일 GPU는 추정상 5Hz에 빠듯하고 10Hz에는 미달. 실시간 가능성은 backbone·구조·하드웨어의 조합으로 판단 | [01](01-research-concept-and-goals.md), [03](03-model-and-training-design.md), [05](05-experiment-and-cloud-plan.md), [06](06-execution-roadmap.md) |
| 로봇 하네스의 주 결정을 결합 행동 후보의 단일 선택으로 변경. 분해형 조건부 질문은 비교군 | 질문 수의 조합 폭발과 수작업 조합 규칙을 피하고 rollout 결과를 후보 라벨로 직접 연결 | [01](01-research-concept-and-goals.md), [02](02-task-and-learning-problem.md), [04](04-data-generation-plan.md) |
| 질문 선행 배치(L1)를 로봇 스트리밍 변형으로 추가하고 연속 틱 지연을 측정 | 로봇은 “같은 질문·바뀌는 상태” 구조라 캐시 대상이 Jev와 반대 | [01](01-research-concept-and-goals.md), [03](03-model-and-training-design.md), [05](05-experiment-and-cloud-plan.md) |
| 규칙 기반 판단기 기준군, 결정 유지 규칙과 안정성 지표 추가. 로봇 데이터 비중 40%→60%, rollout 근거 라벨 확대 | 하네스만으로 풀리는 범위를 먼저 재고, 틱마다 흔들리는 결정을 억제·측정하며, 주 결정 라벨을 실제 결과에 근거시키기 위해 | [02](02-task-and-learning-problem.md), [04](04-data-generation-plan.md), [05](05-experiment-and-cloud-plan.md), [06](06-execution-roadmap.md) |

같은 날의 첫 검토에서 지적한 P0/P1 기준 예산·일정 재산정과 기준군의 2×2 재구성(출력 방식 × 공유 유무)은 위 변경으로 일부만 해소되었고 아직 반영하지 않았다.

같은 날 OM-1의 비동기 계층 구조를 검토한 뒤 robojev의 계층을 "의미에 대한 System 1"(L2)로 정의하고, 전술·그리퍼 상태·정지·거친 운동 의도를 10Hz 스트리밍 typed 출력으로 내는 입출력·데이터 계약을 [08](08-streaming-io-and-data-contract.md)에 합의했다. [리뷰 09](09-streaming-contract-review.md)의 S1~S7(L1-a 격리 계약, truncated BPTT, commitment 기준 부가 출력, 그리퍼 상태 출력, 명령 수명, 라벨 규칙, DAgger 이력 보존)을 08에 반영했다. 이어 08의 11절 목록에 따라 01~06의 로봇 부분(계층·스트림 모드·격리 예외, 첫 작업·질문 세트 v0·조합 규칙·컨트롤러 계약, L1-a 배치·TBPTT·부분 라벨 손실, 에피소드 레코드·D1 규모·rollout 예산·라벨 규칙, 10Hz·50Hz·명령 수명 지표, Task 구성)을 교체했다. 충돌하는 세부는 08이 정본이다. 이어 앞단을 "공통 구조화 스키마를 채우는 교체 가능한 모듈"로 정의하고 실로봇의 주 앞단을 3D 카메라 → 3D 재구성으로 두어, 08(3D 요약 필드·소스별 관측 나이·`geom[]` 슬롯·기하 나이 허용), 05(E2/E3 경로), 06(`perception/` 모듈), 01·03(Physical State → typed Action)을 갱신했다. 마지막으로 07의 R4~R6과 첫 검토의 예산 재산정을 반영했다: 비용 표현 정정(03 §3, 01 §6), 출력 방식 × 공유 유무의 2×2 실험표와 6 checkpoint 정의(03 §5·§6), 2단계 backbone 선정 게이트(06 Task 2b, 05 G0a/G0b), 스트림 기준 시간 풀 재산정(05 §6: 9B는 seeds까지 264 node-hours 안, 27B는 seeds 없이 1~3순위까지). 미반영 항목은 없다.

가설·잠정 목표·측정 결과를 구분한다. 하네스가 구성할 내용과 모델이 학습할 내용을 분리하고 각각의 버전을 기록한다.
