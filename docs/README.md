# 병렬 판단 모델 연구 문서

이 폴더는 연구 설계 문서와 외부 검토의 색인이다. 프로젝트 소개는 뿌리의 [README.md](../README.md)를 본다. [개발 계획서 v2](../Steerix_Robotics_Jev_Development_Plan_v2.docx)를 출발 자료로 삼으며 현재 연구 방향은 아래 문서에 반영한다.

**하네스가 상태·분야별 질문·타입·후보를 구성하고, 모델은 여러 질문의 선택·확률·점수를 병렬로 반환한다. 하네스가 답을 조합해 실행한다.** 모델이 질문과 후보를 생성하는 역할까지 맡지는 않는다.

직접 개발할 범위는 모델 구조, 학습 데이터 생성 파이프라인, 실제 학습·체크포인트, 판단 평가와 로봇 폐루프 검증 전체다. 로봇은 첫 응용이며 질문 의미와 후보를 특정 행동 목록에 고정하지 않는다.

## 현재 문서

| 문서 | 내용 | 상태 |
| --- | --- | --- |
| [연구 컨셉과 목표](01-research-concept-and-goals.md) | 병렬 판단 모델과 하네스의 책임, 입출력, 연구 목표와 비교 원칙 | 연구 설계 초안 |
| [첫 로봇 하네스와 학습 문제](02-task-and-learning-problem.md) | 대표 작업, 질문 묶음, 답의 조합·실행, 데이터 생성과 실제 학습 | 단일 팔·평행 그리퍼를 전제로 한 초안 |
| [모델 구조와 학습 설계](03-model-and-training-design.md) | backbone 후보, 공유 prefix·질문 분기·후보 readout, 손실, 실제 학습 단계와 기준군 | backbone 확정(2026-09-20, Qwen3.5-2B + fused; §"지연 예산" stream 실측 표·§7 상태) |
| [데이터 생성 계획](04-data-generation-plan.md) | 다분야 질문 묶음, 라벨 근거, 시뮬레이션 결과, split·QA, 5K→50K와 반복 수집 | 제작 계획 |
| [실험 환경·GPU 클라우드·비용](05-experiment-and-cloud-plan.md) | 시뮬레이터, 학습 환경, GPU 메모리·가격·비용, checkpoint·평가 | §4 구간 메모리 실측(2B/4B), §6 확정 backbone 기준 재산정(파일럿 T0/LoRA는 Spark) |
| [첫 학습 파이프라인 실행 계획](06-execution-roadmap.md) | 파일·인터페이스·인수 검사, 최초 학습까지의 순서, 12주 산출물과 판단 기준 | Task 2b 2단계(G0b) 완료 — backbone 확정 |
| [설계 변경 검토](07-design-change-review.md) | 다섯 가지 변경에 대한 외부 검토(R1~R6)와 공개 근거 해석 정정 | 검토 결과, R1~R6 반영 완료 |
| [스트리밍 입출력·데이터 계약](08-streaming-io-and-data-contract.md) | robojev의 계층(L2), 10Hz 스트림 입력(계약 v0.3: 변화분 틱·**서식 v0.4** — 풀어 놓은 목표가 모델의 입력에 없다, Task R1), 10개 질문 세트(K≤12), 조합 규칙, 컨트롤러 계약, 라벨(비용 허용 집합·hold∉A)·레코드 정의, 01~06 반영 목록 | **계약 v0.3**(2026-09-19, HANDOFF 결정 1·2). 로봇 스트림의 정본이며 01~06에 반영 완료 |
| [스트리밍 계약 검토](09-streaming-contract-review.md) | 08에 대한 외부 검토(S1~S7)와 수치 검산 | 검토 결과, 08에 반영 완료 |
| [현재 구현 리뷰](10-current-implementation-review.md) | 구현 상태 외부 검토: 재현된 결함 I1~I6, E0 폐루프 연결 점검, 비로봇 2K 재생성 QA, 우선순위 | 검토 결과. I1~I6 모두 타당 확인(47258d6에서 재현). 계약 보강은 08·02에 반영, 코드 수정은 Task 3b 수정 라운드 |
| [DGX Spark 인계 검토](11-dgx-spark-handoff-review.md) | main merge 뒤 외부 검토: 재개 정체 검사(S1), 미지원 model_id(S2), 인계 자료 전달(S3), 토큰 실측과 예산(S4), tokenizer 재현(S5) | 검토 결과. S1~S5 모두 타당. S3·S4는 `HANDOFF.md`·03·05·08에 반영, S1·S2·S5는 코드 수정(fix/handoff-review-11) |

## 상태와 다음 단계

프로젝트의 취지·핵심 아이디어·검증된 것과 아직 아닌 것·로드맵은 저장소 뿌리의 [README.md](../README.md)(영어)에 있다. 그 문서의 Status 표가 태스크 상태의 정본이며, 다른 머신에서 이어가는 절차는 [HANDOFF.md](../HANDOFF.md)에 있다.

## 설계 변경 이력 (요약)

| 시점 | 변경 | 근거 |
| --- | --- | --- |
| 2026-09-18 근본 검토 | readout을 후보별 분기에서 결정 위치 pointer로; backbone을 지연 예산에서 역산; 주 결정을 결합 행동 후보의 단일 선택으로; 질문 선행 배치(L1)와 연속 틱 지연; 규칙 기준군·결정 유지 규칙·안정성 지표; 로봇 데이터 60% | Jev 핵심 컨셉의 재현 가능성과 실시간 적합성 검토 |
| 2026-09-18 OM-1 검토 → 08 | robojev를 L2 "의미에 대한 System 1"으로 정의, 10Hz 스트리밍 typed 출력 계약(질문 세트 v0·조합 규칙·컨트롤러 계약·라벨·레코드), 앞단을 공통 구조화 스키마를 채우는 교체 가능 모듈(주 앞단 3D 재구성)로 | [08](08-streaming-io-and-data-contract.md), 리뷰 [09](09-streaming-contract-review.md) |
| 2026-09-18 리뷰 10 | 전환 틱 초기 경로·실제 명령 구간 대조·국면별 목표점·적용 시각 기준 기하 나이·기하 나이 기준 실행 가능성·관측된 변위 기준 사건·q_instr/q_observe 귀속·관측 자세 이동 | [10](10-current-implementation-review.md): I1~I6 재현, E0 폐루프 원인(시점 배치·자기 가림) |
| 2026-09-19 구현 실측 | 결정 표지 토큰 규칙(03 §3), 도중 지시 조각은 틱 토큰(08 §3.1), 로봇/비로봇 60%는 step 유효 loss 비중(04), 밀기 국면의 기하 나이 예외(08), 전문가 에피소드의 네 출력 필드(08 §8), 버전·지문·DAgger id 규칙 | 4a/4b/5-CPU/3c 리뷰 |
| 2026-09-19 Spark 실측(Task 2b-G0a) | 배포 장비(DGX Spark GB10)에서 backbone 후보의 native BF16 지연 선별: 현재 서식은 전 후보가 80 ms의 ≥7×, 500토큰 틱에서 2B만 사정권(≈83~123 ms) → G0b 후보 2B(주)·4B(5 Hz 대비), 9B·27B 제외(HANDOFF 결정 3 확정); 03·05·06·08·11·README·HANDOFF 갱신 | `artifacts/reports/backbone-screen.json`, `.superpowers/sdd/task-2b-g0a-{report,review-1}.md` |
| 2026-09-19 리뷰 11·cua-s1 검토 | 토큰 실측(틱당 1,764~3,712)과 엣지 배포 전제로 지연·학습 구간 예산 정정(03·05·08); 소형 scorer 기준군(Task 2c)·문맥 섞기 대조군·개념 수준 holdout·혼동 후보 규칙 | [11](11-dgx-spark-handoff-review.md) |
| 2026-09-19 계약 v0.3 (Task v0.3, 결정 1·2) | 서식 v0.3(짧은 이름, 물체 소개/동적 분리, 변화분 틱, 키 기반 후보 줄; 10물체·K=12에서 틱당 p50 392·p95 662·첫 틱 943), 후보 공간(프로파일 없는 결합 키, 영역 쪽 밀기, K≤12 + 지시 조합 예약; E1 퇴화 틱 49.7% → 0%, 완료 20/26 → 23/26), 비키프레임 비용 허용 집합(τ=0.15)과 hold∉A, holdout 봉인(템플릿 변형·개념 계열, ood_dev/ood_test, 누출 QA; 04 §5 표), batch-0 재생성, Spark 2B·4B 재실행 | [08](08-streaming-io-and-data-contract.md), [04 §5](04-data-generation-plan.md), `.superpowers/sdd/decisions-1-2-v03.md`, `task-v03-contract-report.md` |
| 2026-09-19 계약 v0.3 리뷰 1 수정 | 로봇 문구 변형을 origin group 해시로(걸친 group 0), OOD 몫을 생성 비중으로 ≈10~15 %에(비로봇 분야별 10.6~12.5 %, 로봇 400편 14.5 %), 변화분 줄이 기본값 복귀(`vis=1`·`yaw=0`)를 적음, `<id> gone` 줄, soft token 슬롯 거절, 후보 줄의 `path=ok`·중복 `clr` 생략(10물체·K12 틱당 p50 332·p95 599·첫 틱 880), Spark 판정에 `passes_10hz_window`와 `deadline_fail`·외삽 표기, E1 sweep의 동일 seed 대조, 놓기 국면의 운반 높이 낙하 결함(seed 15·19) 이월 | [08](08-streaming-io-and-data-contract.md), [04 §5](04-data-generation-plan.md), [03](03-model-and-training-design.md), `.superpowers/sdd/task-v03-contract-review-1.md`, `task-v03-fix-round-1-report.md` |
| 2026-09-19 D1-prep (놓기 규칙, 대조 쌍) | 놓기점을 영역 안의 빈 자리로(관측된 바닥 높이, 명령의 `place_mm`, hold·retreat 틱에는 open 없음; h0.5·e0.4·c0.6 — E1 sweep 23/26 → 26/26, 놓기 높이 위 이벤트 0), batch-0 재생성; 대조 sibling·삭제 검사(04 §3·§6) | [08 §4](08-streaming-io-and-data-contract.md), [04 §3](04-data-generation-plan.md), `.superpowers/sdd/task-d1-prep-report.md` |
| 2026-09-20 Task 2b G0b (backbone 최종) | 실제 `stream` 경로(정적 윈도우 KV·마스크 없는 flash·배치 결정 분기·pointer readout)를 Qwen3.5-2B/4B에 구현하고 Spark에서 실측: 2B baseline upper p95 83.8 ms(3.8 ms 미달) → `fused`(몸통+분기 한 forward) 40편 재실측 62.7 / 60.2 ms(`all` 52.5 / 52.0)로 10 Hz 통과, 4B는 5 Hz만; 귀속 측정(절편 = weight-read) → FP8-9B 닫음; readout-only T0·짧은 LoRA·무학습 점수·위치 편향·문맥 섞기·규칙 기준군 평가; 10초 구간 메모리(층 checkpointing 필수, 통합 메모리 울타리); **backbone = 2B + fused**, 4B 5 Hz 대비; 03·05·06·08·README·HANDOFF 갱신 | `artifacts/reports/backbone-stream{,-levers}.json`, `backbone-selection.json`, `.superpowers/sdd/task-g0b-report.md` |
| 2026-09-20 Task D1 (첫 데이터 버전) | 이월 수정 h0.7(정체 감시 절대 상한·밀기 stand-off 여유·zone_full 기록·A/B 드라이버; 04 §3 지시 대조 wording), D1 로봇 400편(36,690틱 — 04 §6의 120K틱 가정을 실측으로 정정)·비로봇 2,000건, 100,640 키프레임 rollout(재개 가능한 묶음 실행)과 계보 버전 `d1-rollout-labels`(`robo_jev.data.lineage`), 대조 레코드의 경로 후보 섞기(QA 정답 위치 검사), Task 2c 소형 scorer 기준군 표(문맥 섞기·치환·규칙 기준군·ECE·선택적 지표; 06 Task 2c), 검수 표본 900문항 + 규약, D-OOD 설정 stub, 공개 세트 변환(평가 전용) | [04 §6](04-data-generation-plan.md), [06 Task 2c](06-execution-roadmap.md), [08 §4](08-streaming-io-and-data-contract.md), `.superpowers/sdd/task-d1-report.md` |
| 2026-09-21 Task D1 fix round 1 (리뷰 1) | 로봇 문맥 섞기 대조군을 **id 재매핑 상태 섞기**로(goal·물체·영역·장면을 다음 에피소드의 것으로, 후보·commitment·실행 상태 유지; 지시 텍스트 섞기는 둘째 열) — 저장한 소형 scorer의 재평가에서 dev/test `q_main` 0.79/0.77 = 상태 섞기 0.78/0.78 > 규칙 0.74(표지 `state_shuffle_irrelevant`·`scorer_beats_rule_judge`), ood_dev 0.40 / 0.38 / 규칙 0.73(06 Task 2c, 08 §10); 놓기 국면의 grasp/place 쌍둥이 키(17,016 rollout, 불일치 1,201:0)를 08 §7 미결로; 검수 표본의 봉인 문항을 별도 파일로; −x 밀기 3.1 %의 원인(08 §4)과 08 §3.4 토큰 측정 출처 | D1 리뷰 1 I1·I2·I3·M2·M7 |
| 2026-09-21 Task P1 (첫 학습 파일럿) | Task 5의 GPU 인수 검사를 실제 2B에서 끝냄(T0 동결 320 tensor 0 변경, LoRA·T1 gradient·parameter 이동, 저장 후 **프로세스 재시작** 재개가 비트 동일 → G0 통과, 200 step profile 9.94 s/step·10.08 GiB) + 06 Task 5의 200 step YAML을 실제로 돌린 설정으로 정정; 프로파일 상한 Q≤16·K≤32을 `validate_record`가 강제(D1 최댓값 스트림 K≤12·비로봇 Q=16); **고정 평가 집합**(`configs/eval/pilot.yaml`, 해시로 run을 묶는다 — 적응 5 run 79d09793eab5, 16틱마다 재는 무학습 2 run은 c772c669c7be)과 그 위에서의 D1 like-for-like 7 run; 06:157(무학습 라벨 점수, **D1만**) 체크; 03 §7의 1·5·7 상태 | [06 Task 5·2b](06-execution-roadmap.md), [03 §7](03-model-and-training-design.md), `artifacts/reports/p1-*.json`, `.superpowers/sdd/task-p1-report.md` |
| 2026-09-21 Task P1 리뷰 1 수정 라운드 | 판정을 대조군에 대고 다시 썼다 — 판정 칸 `robot/ood_dev` q_main(844틱 = 8편)에서 **자기** 상태 섞기 대조군 위 여유는 2B LoRA +0.168 · 2B T0 +0.165 · 2B T1 +0.111 · 4B LoRA +0.098 · **4B T0 +0.072**로 원값이 가장 높은 줄이 가장 작고, 4B T0는 목표·상태를 갈아 끼워도 0.799(규칙 기준군 0.821)이며 **어느 run도 지시 섞기 대조군을 못 넘는다**(±0.014) → **D1 파일럿 규모에서는 어느 적응 모델도 목표를 읽는다는 것을 보이지 못하고, 5 Hz로 갈 품질 근거가 없다**(backbone 결정은 그대로); 모집단·불확실성 명시(규칙 기준군 부분집합 0.821 대 분할 전체 0.731, 편 단위 구간은 레코드별 예측이 없어 계산 불가); 대조 쌍 수 정정(지시 6쌍 중 0~1쌍, `_all` 20쌍 중 0~2쌍); T1 이동 정정(표본 8개 중 **7개** 이동, embedding을 빼면 40 step·1e-5에서 **23.32 %** 이동, norm tensor 66~92 %); 평가 집합 해시가 **점수를 매긴** 집합을 덮게(`tick_stride`) | [06 Task 2b·5](06-execution-roadmap.md), [03 §7](03-model-and-training-design.md), `artifacts/reports/backbone-selection.json`, `.superpowers/sdd/task-p1-review-1.md` |
| 2026-09-22 Task P3 (평가 모집단·주 지표) | **자를 고쳤다.** 판정 칸의 모집단을 `ood_dev` 8편 844틱 → **24편 전부 2,530틱**으로(에피소드를 통째로; 틱은 솎지 않는다 — 스트림은 틱 0부터 재생하므로 솎기가 GPU를 아끼지 못하고 뒤를 자르면 읽기 층 525틱의 251틱이 사라진다), **주 지표를 정답이 그 틱의 현재 commitment가 아닌 층**으로 제도화(08 §10에 정의, 산출물에 `primary_stratum`), **기계적 기준군**을 상시 열로(아무것도 읽지 않고 0.875/0.398 — 넘지 못하는 주장은 주장이 아니다), **commitment 섞기** 셋째 대조군을 만들어 재고 **표준 열은 바꾸지 않기로**(날것의 굴리기는 이 틱의 후보 목록에 16.9 %만 들어맞고, commitment 층에서는 저장된 정답을 거짓으로 만든다), 편 균등 평균을 틱 가중과 나란히. 일곱 run 재측정 결과: fp32 T1의 읽기 층 여유 **+0.257 → +0.080 [+0.022, +0.165]**(살아남되 3분의 1이고 한 편을 빼는 24번 모두 0을 제외한다), 줄어든 몫의 큰 부분은 **상태 섞기 대조군이 기증자에 달려 있어서**다(같은 249틱을 24편 회전으로 재면 +0.024; 모델 열은 844틱 전부 동일, 대조군 열은 9.7 %가 다르다), 여유의 출처는 **`grasp` 61틱 한 갈래**이고 **관측 게이트 210틱에서는 어느 줄도 자기 대조군을 넘지 못한다**. T1 재개 허용 오차를 두 run 기준선에서 **사전 등록**(+ 상대 L2의 0 근처 분모 가드) | [08 §10](08-streaming-io-and-data-contract.md), [06 Task 2b·6](06-execution-roadmap.md), [03 §6](03-model-and-training-design.md), `configs/eval/p3-*.yaml`, `artifacts/reports/p3-*.json`, `.superpowers/sdd/task-p3-report.md` |
| 2026-09-22 Task R1 (과제 재설계 1) | **재료를 고쳤다.** (a) 모델의 입력에서 **풀어 놓은 목표를 뺐다** — `goal` 줄은 버전과 주기적 지시 문장뿐, 물체 소개 줄에 `attr=` 없음(`ts0.6`·서식 v0.4, 08 §3.2); 대신 지시 문장이 취약·금지 물체를 전부 부른다(`s0.3`, 템플릿 id 명시). 두 layout·실제 에피소드에서 누출 표지 11개가 한 줄도 없음을 시험으로 고정; 토큰 −10/틱. (b) 에피소드를 **사건이 잦게**(04 §3): 지시 변경 1~5회, 외란이 대상을 밀고, 꼬리 1 s → 0.3 s, 틱 안 사건 복원, 정체 감시(`h0.9`)와 앞단의 자기 가림 이어 들기(`pw0.2`). (c) **사건을 재는 지표 넷**과 계기 수리 두 가지(지시 섞기의 승격, 상태 섞기 기증자 고정 제거; 08 §10). **수정 라운드 1(2026-09-23)에서 코퍼스를 다시 만들었다**(`r1-robot-v0.2`, 400편 **42,509틱**): 첫 판은 45초를 정체 순환에 쓴 27편이 읽기 틱의 **60.6 %**를 차지했고 감시가 그것을 감췄다(리뷰 1 C1). h0.9가 순환을 끊고 못 끊으면 `stall_exhausted`로 끝낸다 — `max_ms` 27 → **0**편, 세계 사건 틱 1.6 → **15.75 %**, `instruction_changed` 줄 7/57 → **564/564**. **정직한 읽기 밀도는 4.15 %**(완료한 368편만 3.34 %)로 목표 8 %에 못 미친다 — 첫 판의 7.8 %는 병적 편이 끌어올린 값이었다 | [08 §3.2·§3.4·§10](08-streaming-io-and-data-contract.md), [04 §3·§5](04-data-generation-plan.md), [03 §6·§7](03-model-and-training-design.md), `.superpowers/sdd/task-r1-report.md` |
| 2026-09-24 Task R4 (폐루프 첫 run) | **학습된 모델을 시뮬레이터 루프에 꽂았다 — 그리고 루프가 오프라인 칸이 못 본 것을 보였다.** `ModelPolicy`(증분 ts0.6 직렬화 + 정적 prefix KV·30틱 윈도우 + 결정 분기 배치 readout)가 녹화 dev 3편 3,890답에서 오프라인 재생과 **확률 차 0**으로 같고(A3), 루프 안 틱 지연 p50/p95/p99 **43.6/56.8/82.7 ms**, 100 ms 초과 0(A4). dev 100·ood_dev 26 새 seed를 expert·규칙·기계적·모델 둘이 같은 seed로 돌았다: expert 0.970 / 1.000, 규칙 0.800 [0.720, 0.870] / 0.692, 기계적 0 / 0, **모델 seed 18 0 / 0**, 466 0.020 / 0(`done ∧ target_inside_zone`으로는 1/100 — 대상이 이미 영역 안이라 expert·규칙도 닫지 않고 완료하는 장면 하나 + 거짓 done 하나; expert·규칙에는 거짓 done이 없다). 모델은 루프에서도 지시를 읽고(채택 행동이 참조와 91.8 % 같고 목표 변경 즉시 반응 95.9 % — 규칙 79.9 %) **파지점에서 `closed`를 한 번도 내지 못한다**(참조 그리퍼 전환 125건 중 실행 0건; 실패 100편 = 부가 판단 90 · 주 결정 8 · 기하 2) — 같은 checkpoint의 오프라인 `q_gripper` 0.996은 닫힌 뒤의 실행 상태를 베끼는 틱의 값이다. 규칙 기준군은 지시 변경 없는 층만 포화(변경 층 expert 대비 −0.213 [−0.312, −0.125]) → 과제 재설계는 G1에 올리지 않는다. 첫 run report(`docs/reports/run-report-1.md`)와 **권고: 클라우드 확대 전에 부가 판단의 라벨·자를 고친다.** GPU 0.65 h | [06 Task 6·3](06-execution-roadmap.md), [08 §10](08-streaming-io-and-data-contract.md), [03 §7](03-model-and-training-design.md), `configs/eval/r4-*.yaml`, `artifacts/reports/r4-*.json`, `.superpowers/sdd/task-r4-report.md` |
| 2026-09-23 Task R3b (클라우드 실행기) | **run을 명세된·상한 있는·감사 가능한 것으로 만들었다 — 클라우드 지출 0.** `infra/run-manifest.schema.json`(`run-manifest-v0`: 코드 리비전 + dirty, 설정 sha256과 푼 설정의 sha256, 데이터 manifest의 경로·sha256, tokenizer·계약 digest, seed·`max_steps`·checkpoint 정책, **상한 셋 + 시간당 단가**, 산출물·상태·진행·종료 이유·장부) + `scripts/launch_run.py {prepare,launch,status,fetch,cancel,resume}` + `src/robo_jev/launch/`(원격 러너·ssh/local 백엔드·공급자 인터페이스). **상한 셋은 하나의 벽시계 마감으로 환산되고 원격 러너가 스스로 잰다**(실행기가 죽어도 지켜진다; 넘으면 checkpoint를 쓰고 `failed(budget)`), `fetch`는 원격이 잰 sha256과 가져온 파일을 다시 잰 값을 대조하며, **`cancel`은 확인 못 하면 `unknown`**이다(종료 코드 3). 인수: localhost SSH 왕복(`tiny_cpu.yaml` 5 step **145.688 s**(러너의 경과; 학습기 시계로는 144.85 s), 다섯 산출물 전부 일치)과 고의 실패 넷(벽시계·USD·`kill -9`·미확인 취소). 공급자 인스턴스 생성·종료는 **인터페이스만**이고 공급자·계정·첫 유료 run은 사용자 결정이다 | [06 Task 6](06-execution-roadmap.md), [05 §6](05-experiment-and-cloud-plan.md), `infra/run-manifest.schema.json`, `.superpowers/sdd/task-r3b-report.md` |

가설·잠정 목표·측정 결과를 구분한다. 하네스가 구성할 내용과 모델이 학습할 내용을 분리하고 각각의 버전을 기록한다.
